"use client";
export default function FriendsSection({
  id,
  userName,
  image,
}: {
  id: string;
  userName: string;
  image: string;
}) {
  return <section className="container">
    
  </section>;
}
